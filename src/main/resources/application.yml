spring:
  application:
    name: "EurokaServer"
server:
    port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: ${eureka_url:http://host.docker.internal:8084/eureka/}

